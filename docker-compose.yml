services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      APP_VERSION: v1.0.0
      SERVICE_NAME: backend

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
